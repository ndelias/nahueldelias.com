---
// §9 step 1 placeholder: exists so the pipeline and budgets have a real page
// to measure. Replaced by the (Index) list and filmstrip in step 4.
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Nahuel D'Elias — Product engineer</title>
    <meta name="description" content="[Identity line]" />
    <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
    <style>
      :root {
        color-scheme: light dark;
        --bg: #F2F1ED;
        --muted: #67645E;
        --ink: #1B1A18;
      }
      @media (prefers-color-scheme: dark) {
        :root { --bg: #161513; --muted: #9A968E; --ink: #EDEBE6; }
      }
      body {
        margin: 0;
        padding: 28px max(16px, 2.2vw);
        background: var(--bg);
        color: var(--ink);
        font: 15px/1.45 system-ui, sans-serif;
      }
      h1 { margin: 0; font-size: inherit; font-weight: 500; }
      p { margin: 0; color: var(--muted); }
    </style>
  </head>
  <body>
    <main>
      <h1>Nahuel D'Elias</h1>
      <p>Product engineer</p>
    </main>
    <script>import "../scripts/ballast";</script>
  </body>
</html>
